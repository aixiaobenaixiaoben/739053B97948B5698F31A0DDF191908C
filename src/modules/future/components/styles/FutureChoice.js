import {StyleSheet} from "react-native"
import {COLOR_BLACK_02} from "../../../../Style"

export default StyleSheet.create({
  modalView: {
    flex: 1,
    alignItems: 'center',
    backgroundColor: COLOR_BLACK_02,
  },
  modalList: {
    width: 200,
    borderRadius: 4,
    overflow: 'hidden',
  }
})